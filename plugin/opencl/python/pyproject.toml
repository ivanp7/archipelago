[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend= "setuptools.build_meta"

[project]
name="archi-opencl"
version="0.1.0"
description="Python bindings for the Archipelago OpenCL plugin"
dependencies=["archi-base", "archi-builtin"]
requires-python=">=3.13"

